server:
  port: 83
spring:
  application:
    name: nacos-order-consumer
  cloud:
      nacos:
        discovery:
          server-addr: localhost:8848        #nacos服务地址
service-url:
  nacos-user-service: http://nacos-payment-provider
  #消费者要去访问的微服务名nacos-payment-provider(注册成功到nacos的微服务提供者)
